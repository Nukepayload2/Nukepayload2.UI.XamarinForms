<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Nukepayload2.UI.Xaml"
             x:Class="Nukepayload2.UI.Xaml.HyperlinkButton">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label TextColor="Blue" Text="{Binding Text}">
            <Label.GestureRecognizers>
                <TapGestureRecognizer x:Name="TblLink"/>
            </Label.GestureRecognizers>
        </Label>
        <BoxView Grid.Row="1" Color="Blue" HeightRequest="1" HorizontalOptions="Fill"/>
    </Grid>
</ContentView>